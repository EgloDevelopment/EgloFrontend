import { writable } from "svelte/store";

export const notifications = writable([]);
export const user = writable([]);
export const services = writable([]);

export const data_to_load = writable([]);
